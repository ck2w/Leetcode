

全源最短路径算法，用于求图中所有节点对之间的最短距离。

- 状态定义
dp[k][i][j] = 只允许经过节点 {1, 2, ..., k} 作为中间节点时，从 i 到 j 的最短距离。


- 状态转移方程
dp[k][i][j] = min(dp[k-1][i][j],  dp[k-1][i][k] + dp[k-1][k][j])

对每个新的中间节点 k，判断"绕道经过 k"是否比"不经过 k"更短。
由于 dp[k] 只依赖 dp[k-1]，可以原地更新，压缩到二维。