# 问题定义
- 给定数组 [10, 9, 2, 5, 3, 7, 101, 18]，找出最长严格递增子序列的长度。
- 子序列不要求连续，只要保持相对顺序且严格递增即可。答案是 [2, 3, 7, 101]，长度为 4。


# 定义状态
- dp[i] = 以 nums[i] 结尾的最长递增子序列长度。

# 转移方程
- dp[i] = max(dp[j] + 1)   对所有 j < i 且 nums[j] < nums[i]

# base case
- 每个元素单独成一个子序列，所以 dp[i] = 1。

# 最终答案
- max(dp)，因为最长子序列可以结尾在任意位置。